# Project Instructions - WikiDSI

## Workflow
- **Git Automation**: After every modification to the codebase, automatically perform:
  1. Update `README.md` if the change introduces a new feature or significant behavioral change.
  2. `git add <file>` or `git add .`
  3. `git commit -m "<Descriptive message of the change>"`
